To make sure we can connect to our network once we boot into this installation, we will need to install a network manager like so:

```bash
pacman -S networkmanager
```

Then all we have to do is enable this on boot with:

```bash
systemctl enable NetworkManager
```

