This will be the name your computer will be recognized as. Simply write the name you want to the hostname file like so:

```bash
echo "arch" > /etc/hostname
```

This will make my computer show up as "arch" on the network's DHCP server and show up in the terminal after our username.